package String;

public class StringMethods {
    public static void main(String[] args) {
        // String Class provides inbuilt  string methods
        //1.toUpperCase() -- uppercase all the character in string
        //2.toLowerCase() -- lowercase all the character in string
        //3.trim() -- remove spaces from string
        //4.startWith() -- checks the string starts with input
        //5.endsWith() -- checks the string ends with input
        //6.equals() -- checks the string content is similar to input
        //7.equalIgnoreCase() -- checks the string content is similar to input ,ignoring case
        //8.charAt() --- return char at given Index
        //9.valueOf() --- converts number to string
        //10.replace() --- replace the given words with another input
        //11.contains() --- check if the string contains the input word
        //12.substring() --- create another string from a string
        //13.split() --- return array created by spliting string as per the input given
        //14.toCharArray() --- returns array of all char in string
        //15.isEmpty() --- check if the string is empty or has no character
    }
}
